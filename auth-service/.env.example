# Copy this file to .env and fill in the actual connection string.
# Use the Render Postgres Internal Connection String for Path A (clean multi-tenant DB).
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE

# Optional shadow DB for Prisma (uncomment if used)
# SHADOW_DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/SHADOW_DB
