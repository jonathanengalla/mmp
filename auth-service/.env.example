# === Auth / JWT ===
# Strong random secret used for signing JWTs in auth-service.
# Override this per environment; NEVER commit a real secret.
JWT_SECRET=changeme-super-long-random-secret

# Algorithm used by the JWT utility. Default is HS256 if unset.
JWT_ALGORITHM=HS256
