#!/usr/bin/env node
/**
 * db:seed:base (idempotent)
 * Insert baseline defaults only if missing. Must be safe for demo/local.
 * Currently a placeholder until persistent storage is wired.
 */

console.log("[db:seed:base] No-op: add insert-if-missing seed logic when DB is configured. Must stay idempotent.");

