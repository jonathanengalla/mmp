# Production API base (Render backend). Do NOT include /api – paths like /auth/login are added by the client.
VITE_API_BASE_URL=https://oneledger.onrender.com

# === Auth / Tenant ===
# Default tenant ID used by the login form.
# Replace with the tenant present in the backend DB.
VITE_DEFAULT_TENANT_ID=rcme-dev
