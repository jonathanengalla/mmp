# === API Base ===
# Local API base for dev (adjust if backend runs on a different port/host)
VITE_API_BASE_URL=http://localhost:3000

# === Auth / Tenant ===
# Default tenant ID used by the login form.
# Set this to the tenant key that exists in your dev DB (e.g., rcme-dev).
# Do NOT hardcode tenant IDs in code; always use env or runtime config.
VITE_DEFAULT_TENANT_ID=rcme-dev
